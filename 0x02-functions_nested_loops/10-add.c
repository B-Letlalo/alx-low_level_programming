#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: first integer
 * @b: second integer
 * Return: returns sum of numbers
 */
int add(int a, int b)
{
	int sum = 0;

	sum = a + b;
	return	(sum);
}
