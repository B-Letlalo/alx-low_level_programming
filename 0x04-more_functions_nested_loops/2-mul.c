#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first argument/ digit
 * @b: second argument/digit
 * Return: returns multiplication of two integers
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return	(result);
}
