#include "main.h"
/**
 * _isdigit - checks for a digit (0 through 9)
 * Return: 1 if c is a digit, 0 otherwise
 * @c: function parameter
 */
int _isdigit(int c)
{
	if (c <= 9 && c >= 0)
	{
		return	(1);
	}
	else
	{
		return	(0);
	}
}
